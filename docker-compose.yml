services:
  pong-front:
    container_name: game-back
    build:
      context: ./game/back
    networks:
      - pong_network
    ports:
      - "8888:8888"

networks:
  pong_network:
    name: pong_network